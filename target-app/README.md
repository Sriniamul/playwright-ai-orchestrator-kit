# Target application

Place the application source here, or change `projectFolder` in `orchestrator.config.json` to its path.
The application must already be running at the configured `appUrl` before orchestration starts.
